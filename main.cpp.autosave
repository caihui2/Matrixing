#include <iostream>
#include "vector.h"
#include "matrix3x3.h"
using namespace std;

int main()
{
    Matrix3X3 m(-4, -3, 3, 0, 2, -2, - 1, 4, -1);
    Matrix3X3 rm = getInverse(m);     
    return 0;
}

